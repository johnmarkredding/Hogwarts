import React from "react";

class House extends React.Component {
  render() {
    return (
      <article className={`${this.props.house}-house`}>

      </article>
    );
  };
};

export default House;
