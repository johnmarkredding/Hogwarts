import React from "react";

const Search = props => {

  return (
    <section className="search-form">
      <input type="text" placeholder="Search by name or house"/>
    </section>
  );
};

export default Search;
